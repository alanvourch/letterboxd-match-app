node_modules
dist
.DS_Store
*.local

# Secrets & config locale (clé TMDB…) — jamais committés
.env
.env.*
!.env.example
.vercel

# Exports Letterboxd personnels (données privées — ne pas committer)
letterboxd-*/
*.zip

# Outillage/config assistants IA (usage local, pas destiné au repo public)
CLAUDE.md
AGENTS.md
.claude/
.codex/
.cursor/
.windsurfrules

.vercel
